# 1. Inisialisasi folder
git init

# 2. Bungkus semua kode (titik artinya semua file yang tidak dilarang .gitignore)
git add .

# 3. Beri catatan upload
git commit -m "Mesin konten siap beraksi"

# 4. Hubungkan ke alamat GitHub (Ganti USERNAME dan REPO dengan punya kamu)
git remote add origin https://github.com/USERNAME_KAMU/NAMA_REPO_KAMU.git

# 5. Kirim ke awan!
git push -u origin main
